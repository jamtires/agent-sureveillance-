{
  "name": "jamtires-monitoring",
  "version": "1.0.0",
  "description": "Surveillance E2E de la plateforme tyre-solution.be",
  "scripts": {
    "test": "playwright test",
    "test:monitoring": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:ui": "playwright test --ui",
    "build:failure-context": "node scripts/build-failure-context.js",
    "analyze:failure": "node scripts/analyze-with-claude.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
